using System;

namespace _011_运算符的优先级
{
    class Program
    {
        static void Main(string[] args)
        {
            //int age = Convert.ToInt32(Console.ReadLine());
            //bool isYoung = (age >= 18) && (age <= 65);
            //Console.WriteLine(isYoung);

            int a = 1 + 2 * 3;
            Console.WriteLine(a);
        }
    }
}
